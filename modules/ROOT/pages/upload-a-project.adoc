= Import a Project into an IDE Workspace
//variable used in multiple places on this page:
:root-dir: This directory is the top-level folder that Anypoint Code Builder generates when it creates a project. Do not navigate to the project’s parent or ancestor directory or to one of its child or descendant directories.
//variable used in multiple places on this page:
:open-root: If you do not open the root directory, the project does not load correctly because Visual Studio Code does not recognize the directory as a workspace, so the UI canvas for the Mule application does not load properly.

include::reuse::partial$beta-banner.adoc[tag="anypoint-code-builder"]

//LOGO (web, desktop, or both)
include::partial$acb-ide-logos.adoc[tags="both-ides"]

Import a project directory or a packaged project into a workspace in your IDE. A project directory contains an API specification or a Mule application for an implementation or integration. 

Visual Studio Code treats a project directory as a workspace when you open it to the project's root directory or if your project folder is located in a https://code.visualstudio.com/docs/editor/multi-root-workspaces[multi-root workspace^]. 

== Import a Project into a Desktop IDE Workspace

The desktop IDE enables you to upload a project directory or package JAR file into a workspace. 

* <<desktop-project-folder>>
* <<desktop-project-package>>

[[desktop-project-folder]]
=== Import a Project Folder into a Desktop IDE Workspace

. Open the Command Palette by pressing Ctrl+Shift+p (Windows) or Cmd+Shift+p (Mac), and provide the following command:
+
[source,command]
----
File: Open Folder...
----
. Navigate to the _root_ directory for your project. 
+
// see text for this variable defined under title
{root-dir}
. Open the root directory for your project.
+
// see text for this variable defined under title
{open-root}

[[desktop-project-package]]
=== Import a Packaged Project into a Desktop IDE Workspace

Import a JAR file and automatically unpack the contents to a project folder.

. Open the Command Palette by pressing Ctrl+Shift+p (Windows) or Cmd+Shift+p (Mac), and provide the following command:
+
[source,command]
----
MuleSoft: Import a Project
----
. Select *Mule Project Type*:
+
image::import-mule-project.png[]
. Navigate to the JAR file and click *Select a jar file to import*.
. Navigate to a parent directory into which you want to store the decompressed project folder and click *Select a folder to work*.
+
The IDE unpacks the JAR file, loads the project folder, and opens the project workspace in the IDE. 

== Import a Project into a Web IDE Workspace

The web IDE enables you to upload a project directory or JAR file into a workspace.

* <<web-project-folder>>
* <<web-project-package>>

//WEB: PROJECT FOLDER TO WORKSPACE
[[web-project-folder]]
=== Import a Project Folder into a Web IDE Workspace

. Open the Command Palette by pressing Ctrl+Shift+p (Windows) or Cmd+Shift+p (Mac), and provide the following command:
+
[source,command]
----
File: Open Folder...
----
+
image::file-open-folder-command.png[]
. Select your Anypoint Code Builder home directory:
+
image::select-home-directory.png[]
. After Anypoint Code Builder loads your home directory, drag your project directory to your Explorer view:
+
image::drag-folder-explorer.png[]
+
If prompted, trust the authors of your file or directory:
+
image::trust-home-directory.png[]
+
Anypoint Code Builder displays an upload progress bar that you can click to expand.

. To load the project into a workspace, open the _root_ directory for the imported project folder.
+
// see text for this variable defined under title
{open-root}

.. Open the Command Palette by pressing Ctrl+Shift+p (Windows) or Cmd+Shift+p (Mac), and provide the following command:
+
[source,command]
----
File: Open Folder...
----
.. Provide the path to the root directory of the project and click *OK* to load the project root directory into the workspace.


//WEB: PACKAGED PROJECT TO WORKSPACE
[[web-project-package]]
=== Import a Packaged Project into a Web IDE Workspace

When you import a JAR file, Anypoint Code Builder automatically unpacks the contents to a project folder.

. Open the Command Palette by pressing Ctrl+Shift+p (Windows) or Cmd+Shift+p (Mac), and provide the following command:
+
[source,command]
----
 File: Open Folder...
----
+
image::file-open-folder-command.png[]
. Select your Anypoint Code Builder home directory:
+
image::select-home-directory.png[]
+
If prompted, trust the authors of the files in the directory:
+
image::trust-home-directory.png[]
. After Anypoint Code Builder loads your home directory, drag your JAR file to your Explorer view:
+
image::upload-jar-file.png[]
If prompted, trust the authors of your file or directory:
+
image::trust-author-upload-file.png[]
+
Anypoint Code Builder displays the upload progress on the bottom left side of your screen:
+
image::upload-progress.png[]
+
You can expand the progress bar by clicking it:
+
image::upload-progress-expanded.png[]
. After the upload process is complete, provide the following command to import the JAR file to your workspace:
+
[source,command]
----
MuleSoft: Import a Project
----
+
image::import-a-project.png[]
.. Select *Mule Project Type*:
+
image::import-mule-project.png[]
.. Select the JAR file that you uploaded.
+
Anypoint Code Builder unpacks your application into a project folder.
. Navigate to your the _root_ of the project folder, using the following command to open the folder and load your workspace to the IDE:
+
[source,command]
----
 File: Open Folder...
----
+
// see text for this variable defined under title
{open-root}


[[use-dw-imported-project]]
== Use DataWeave with Imported Projects

To use DataWeave in an imported project, a `mule-service-weave-ee` dependency must be configured in your project `pom.xml` file, for example:

[source,XML]
----
<dependency>
  <groupId>com.mulesoft.services</groupId>
  <artifactId>mule-service-weave-ee</artifactId>
  <version>2.4.0-20220824</version>
  <scope>runtime</scope>
</dependency>
----
