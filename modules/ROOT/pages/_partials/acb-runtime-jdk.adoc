//
// tag::runtime-jdk-versions-supported[]
//TBD IF WE USE THIS

|===
| Mule runtime version | JDK version 
| Mule 4.6 | JDK 1.8.0, JDK 11, JDK 17
| Mule 4.5 | JDK 1.8.0, JDK 11
| Mule 4.4 | JDK 1.8.0, JDK 11
|===

// end::runtime-jdk-versions-supported[]
//


//
// tag::runtime-jdk-download[]
You can select any of the supported versions. When you create the project, the IDE automatically downloads selected Mule and JDK versions that append the message *(downloads upon project creation)* and saves the settings to the project's `mule-artifact.json` file.
include::anypoint-code-builder::partial$acb-runtime-jdk.adoc[tags="runtime-jdk-defaults"]
// end::runtime-jdk-download[]
//

//
// tag::runtime-jdk-defaults[]
//TODO: 
//      awaiting publication of setting PR as of 04/24: 
//      https://github.com/mulesoft/docs-code-builder/pull/256/files
To set default Mule runtime and JDK versions for the projects you create, see xref:ref-mule-settings.adoc[].
// end::runtime-jdk-defaults[]
//

//
// tag::runtime-jdk-notification[]
The IDE provides a notification if it is necessary to download the selected runtime or JDK version for the project. Mule runtime downloads to `${user.home}/runtimes`, and the JDK downloads to `${user.home}/jdk`.
// end::runtime-jdk-notification[]
//

//
// tag::runtime-jdk-missing[]
If a Mule runtime or JDK configuration is missing, set the versions to use from the canvas. 
// end::runtime-jdk-missing[]
//